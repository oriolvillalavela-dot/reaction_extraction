# SURF Extractor – Python dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0

# ── AI Gateway ─────────────────────────────────────────────────────────────────
portkey-ai>=1.9.0

# ── Data validation ────────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── PDF text extraction (used by MERMaid wrapper) ──────────────────────────────
pdfplumber>=0.11.0
PyMuPDF>=1.24.0           # fallback PDF reader (package name: fitz)

# ── Chemistry / ChemConverter dependencies ────────────────────────────────────
rdkit>=2024.3.1            # or install via conda: conda install -c rdkit rdkit
cirpy>=1.0.2               # CIRpy for IUPAC→SMILES via CAS Chemical ID Resolver
requests>=2.31.0

# ── MERMaid / VisualHeist dependencies (optional; heavy ML stack) ─────────────
# Uncomment if you want VisualHeist image extraction.
# torch==2.3.0
# torchvision==0.18.0
# transformers>=4.41.0
# safetensors>=0.4.3
# huggingface_hub>=0.23.0
# einops>=0.7.0
# timm>=0.9.16
# pdf2image>=1.17.0       # requires poppler-utils system package
# Pillow>=10.3.0
# opencv-python>=4.9.0
# numpy>=1.26.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
