# SURF Extractor – Python dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0

# ── AI Gateway ─────────────────────────────────────────────────────────────────
portkey-ai>=1.9.0

# ── Data validation ────────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── PDF text extraction ────────────────────────────────────────────────────────
pdfplumber>=0.11.0
PyMuPDF>=1.24.0           # package name: fitz

# ── Chemistry (vendored ChemConverter) ────────────────────────────────────────
# CASClient requires CAS_CLIENT_ID / CAS_CLIENT_SECRET / CAS_TOKEN_URL / CAS_SERVER
# in your .env.  Without them CASClient is skipped and CIRpy is used as fallback.
rdkit>=2024.3.1            # or: conda install -c rdkit rdkit
cirpy>=1.0.2
requests>=2.31.0

# ── VisualHeist (vendored MERMaid) – heavy ML stack ───────────────────────────
# Uncomment to enable image-based table extraction from PDFs.
# On Windows also set POPPLER_PATH env var to your Poppler binary directory.
# torch>=2.3.0
# torchvision>=0.18.0
# transformers>=4.41.0
# safetensors>=0.4.3
# huggingface_hub>=0.23.0
# einops>=0.7.0
# timm>=0.9.16
# pdf2image>=1.17.0       # requires poppler-utils system package
# Pillow>=10.3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
