# Vendored dependencies bundled inside surf_extractor.
# ChemConverter (cas_client, converters) and VisualHeist live here so the
# app is fully self-contained and does not require sibling repo checkouts.
